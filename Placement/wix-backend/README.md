# Brighton Placement Wix backend

Final contract: `2026-09-19.7`

Copy the complete contents of:

`http-functions.js`

into the Brighton Wix site's backend `http-functions.js` file, merging shared imports only if that Wix file already contains other HTTP functions.

This backend includes:

- start/resume;
- idempotent adaptive module submission;
- private answer-key grading;
- Reading and Listening routing;
- deterministic Speaking grading;
- Speaking error skip;
- final result reconstruction for the certificate-style result screen.

CMS collection definitions and CSV instructions are in:

`../WIX_SETUP.md`
